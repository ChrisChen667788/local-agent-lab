# v0.2.2 · 2026-03-28

## Scope

- 收口 `v0.2.1` 之后的版本可见性、Benchmark 可信度、本地运行稳定性、知识库可用性与发布纪律，完成 post-`v0.2.1` 二十轮主线。

## Included

- UI / workflow updates:
  - 顶栏已显示当前稳定版本 `v0.2.2`
  - `/agent` 与 `/admin` 第一屏做了最终间距与层级审计，继续向验收截图靠拢
  - `/agent` 补了更清晰的本地运行阶段态显示
  - `/admin` 补了选中 benchmark 目标的当前实际模型版本摘要
  - Benchmark 结果、历史、delta、失败摘要与失败热点分布继续统一阅读口径
- Runtime / benchmark updates:
  - `/api/admin/dashboard` 对未配置远端 key 的目标改成安全解析，不再因为未启用 provider 触发 `500`
  - full-suite 远端长跑稳态优化已验证：
    - `terminated: 33 -> 0`
    - `total failed: 37 -> 1`
    - 剩余唯一失败为单个 `502 Bad Gateway`
  - 本地 `milestone-formal` 已重跑回归：
    - `Local Qwen3 0.6B = 58/58`
    - `Local Qwen3 4B 4-bit = 58/58`
    - 总计 `116/116`、`0 failed`
  - Benchmark 支持更明确的运行中恢复监控、停止/放弃控制、队列可视化与失败热点 drilldown
- Retrieval / grounding updates:
  - 知识库已补目录/路径导入工作流
  - citation 点击回看与 grounded 结果解释性继续收口
  - 会话导出与 benchmark 导出包字段一致性继续统一
- Release / operations updates:
  - 新增服务端会话快照路由
  - 新增 smoke test 脚本
  - 发布流程补齐 `v0.2.x` cadence 与 `v0.3.0` preread 文档

## Verification

- TypeScript: passed (`tsc --noEmit`)
- Smoke test: passed (`./scripts/smoke-test.sh`)
- Agent URL: [http://localhost:3011/agent](http://localhost:3011/agent)
- Admin URL: [http://localhost:3011/admin](http://localhost:3011/admin)
- Remote full-suite stability comparison:
  - old run: `7cda7c5e-b783-4a0c-be86-5b8fd43905c2`
    - `failed=37`
    - `terminated=33`
    - `aborted=3`
    - `502=1`
  - new run: `ff0fa2b1-b735-4453-ba30-a5f8b7049a51`
    - `failed=1`
    - `terminated=0`
    - `aborted=0`
    - `502=1`
- Local formal regression:
  - run: `93911b58-58eb-4165-b39a-41ef8224542e`
  - `Local Qwen3 0.6B: 58/58`
  - `Local Qwen3 4B 4-bit: 58/58`
  - combined: `116/116`, `0 failed`

## Screenshots

- Agent:
  - use current `/agent` page as the acceptance reference
- Admin:
  - use current `/admin` page as the acceptance reference

## Notes

- `v0.2.2` 代表 post-`v0.2.1` 二十轮主线的收口节点，重点是把“可用、可观测、可发布、可回看”做实，而不是继续扩张结构。
- 当前主导航工作流已稳定收敛到 `/agent` 与 `/admin`；仓库中仍保留少量历史 rescue / 旧结构文件，但已不阻塞主链路。
- 后续如果继续推进，建议保持 `v0.2.x` 小步节奏；更大的结构性能力升级放到 `v0.3.0` 再统一评估。
