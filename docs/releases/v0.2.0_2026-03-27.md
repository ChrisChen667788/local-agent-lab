# v0.2.0 · 2026-03-27

## Scope

- 收口 20 轮主线开发任务，并把当前版本整理成可验收、可回看、可继续小步优化的稳定节点。

## Included

- UI / workflow updates:
  - `/agent` 第一屏回到“三栏工作台”主结构
  - `/admin` 第一屏回到 Benchmark 优先工作流
  - Benchmark 结果、历史、baseline、delta 统一成长卡片阅读方式
  - 顶部导航、多语言条、右侧信息面板、左侧目标列表都按历史截图主线收口
- Runtime / benchmark updates:
  - 本地网关恢复链路、4B 冷态降级、简单问答优先走 `0.6B`
  - 本地 / 远端 benchmark 口径拆分
  - long-running benchmark 支持基于最新未完成记录恢复监控
  - `Local Qwen3 0.6B` 正式里程碑评测已恢复正常
  - `OpenAI` 远端目标拆成 `OpenAI Codex` 与 `OpenAI GPT-5.4`
- Retrieval / grounding updates:
  - 检索增强误伤普通问答的问题已收口
  - grounded / citation / verification 改成轻量展示
  - 会话导出与筛选逻辑已统一收口

## Verification

- TypeScript: passed (`tsc --noEmit`)
- Agent URL: [http://localhost:3011/agent](http://localhost:3011/agent)
- Admin URL: [http://localhost:3011/admin](http://localhost:3011/admin)
- Runtime availability: `agent=200`, `admin=200`
- Benchmark summary:
  - `Local Qwen3 0.6B` `milestone-formal`: `runs=58`, `okRuns=58`
  - latest unfinished progress recovery verified during a live long-running benchmark
- Acceptance checklist:
  - [acceptance-checklist_2026-03-27.md](../acceptance-checklist_2026-03-27.md)

## Screenshots

- Agent:
  - use current `/agent` page as the acceptance reference
- Admin:
  - use current `/admin` page as the acceptance reference

## Notes

- Current focus after this tag should shift from structural rewrites to stable operation and small, reviewable refinements.
- Historical `rescue` pages remain in the repo for fallback/debugging, but the main workflow is now `/agent` and `/admin`.
- Local MLX cold-start latency is still environment-sensitive; current fixes handle recovery and degradation, not physics.
